export const encryptFile = (filePath: string) => {
  // takes a file path and encrypts it and returns the encrypted file path relative to the uploads folder
  return filePath;
};

export const decryptFile = (encryptPath: string) => {
  // takes an encrypted file path and decrypts it and returns the decrypted file path relative to the uploads folder
  return encryptPath;
};
